# cacheshfs-core

Shared contracts and platform-neutral filesystem/cache types.

Implement the cache-backed VFS here. Platform crates call `VirtualFilesystem`; transport crates implement `RemoteFilesystem`.
